<section class="hero-medicos">
  <div class="hero-topbar">
    <!-- TODO: swap for a dark/light logo variant suited to a white background -->
    <img
      src="/images/logo.png"
      alt="Simbora Maranhão"
      style="height: 36px; max-height: 36px; width: auto; max-width: 160px; display: block; object-fit: contain;"
    />
    <span class="hero-eyebrow">Para médicos que querem crescer online</span>
    <div></div>
  </div>

  <div class="hero-body">
    <h1 class="hero-title">
      Seu nome na cabeça do paciente<br />
      antes mesmo da consulta.
    </h1>

    <p class="hero-subtext">
      A Simbora constrói sua autoridade digital em 90 dias — com gestão de redes,
      conteúdo estratégico e vídeos que posicionam você como referência na sua especialidade.
    </p>

    <div class="hero-video">
      <div class="hero-video-inner">
        <span class="hero-play">▶</span>
        <span class="hero-video-label">Vídeo de apresentação</span>
      </div>
    </div>

    <a href="https://wa.me/5598999999999" target="_blank" rel="noopener noreferrer" class="hero-cta">
      Quero construir minha autoridade →
    </a>

    <p class="hero-note">Sem contrato de fidelidade. Resultado em 90 dias.</p>
  </div>
</section>

<style>
  .hero-medicos {
    background: #ffffff;
    padding: 40px 40px 80px 40px;
  }

  .hero-topbar {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    width: 100%;
    margin-bottom: 60px;
  }

  .hero-topbar img {
    justify-self: start;
  }

  .hero-body {
    text-align: center;
    max-width: 860px;
    margin: 0 auto;
  }

  .hero-eyebrow {
    display: inline-block;
    background: rgba(0, 200, 200, 0.08);
    border: 1px solid rgba(0, 200, 200, 0.2);
    color: #00c8c8;
    font-family: "Inter", sans-serif;
    font-weight: 500;
    font-size: 0.8rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    padding: 8px 20px;
    border-radius: 8px;
    margin-bottom: 0;
    width: fit-content;
    justify-self: center;
  }

  .hero-title {
    font-family: "Playfair Display", serif;
    font-weight: 400;
    font-size: clamp(2.2rem, 4vw, 3.5rem);
    color: #0d0d0d;
    line-height: 1.1;
    margin-bottom: 16px;
  }

  .hero-subtext {
    font-family: "Inter", sans-serif;
    font-weight: 400;
    font-size: 1.1rem;
    color: #6b7280;
    line-height: 1.7;
    max-width: 640px;
    margin: 0 auto 28px;
  }

  .hero-video {
    width: 100%;
    max-width: 760px;
    aspect-ratio: 16 / 9;
    background: #f3f4f6;
    border-radius: 16px;
    border: 1px solid #e5e7eb;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 28px;
  }

  .hero-video-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 12px;
  }

  .hero-play {
    color: #00c8c8;
    font-size: 3rem;
  }

  .hero-video-label {
    font-family: "Inter", sans-serif;
    font-weight: 400;
    font-size: 0.9rem;
    color: #9ca3af;
  }

  .hero-cta {
    display: block;
    width: fit-content;
    margin: 0 auto;
    text-align: center;
    background: #00c8c8;
    color: #000000;
    font-family: "Inter", sans-serif;
    font-weight: 600;
    font-size: 1rem;
    padding: 18px 48px;
    border-radius: 8px;
    border: none;
    text-decoration: none;
    box-shadow:
      0 1px 0 rgba(255, 255, 255, 0.2) inset,
      0 -1px 0 rgba(0, 0, 0, 0.15) inset,
      0 4px 20px rgba(0, 200, 200, 0.25);
    transition: all 0.2s ease;
  }

  .hero-cta:hover {
    transform: translateY(-1px);
    box-shadow:
      0 1px 0 rgba(255, 255, 255, 0.2) inset,
      0 -1px 0 rgba(0, 0, 0, 0.15) inset,
      0 8px 28px rgba(0, 200, 200, 0.35);
  }

  .hero-note {
    font-family: "Inter", sans-serif;
    font-weight: 400;
    font-size: 0.8rem;
    color: #9ca3af;
    text-align: center;
    margin-top: 12px;
  }

  @media (max-width: 640px) {
    .hero-medicos {
      padding: 24px 20px 60px 20px;
    }

    .hero-video {
      max-width: 100%;
      border-radius: 8px;
    }

    .hero-cta {
      width: 100%;
      max-width: 480px;
      padding: 16px 24px;
    }
  }
</style>
